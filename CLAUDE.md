# Mālama Map — プロジェクト引き継ぎ & 将来構想ロードマップ

**Mālama Map**（「Mālama」＝ハワイ語で「守る・世話をする」）
ハワイ在来植物の目撃情報を市民が投稿・共有し、外来種の侵食と ROD（Rapid ʻŌhiʻa Death）を早期発見する Web アプリ。

- 目的：①アメリカ大学受験の実績（Common App）②U22プログラミングコンテスト一次審査通過
- 制作者：高校生（APIS / ハワイ）。会話は日本語。HTML/CSS/JS 可、Firebase/TF.js は未経験。

---

## 現状（フェーズ1 で実装済み）

サーバーレス構成・**バニラ HTML/CSS/JS**（フレームワークなし）。データは今のところ **localStorage**（DB未導入）。

| ファイル | 内容 |
|---|---|
| `index.html` / `map.js` | Leaflet 地図（**ハワイ限定** bounds・余白は海色）／ピン（緑=在来 赤=外来 白=ROD 灰=未確認）／ポップアップ（写真・種名・AI%）／最新投稿フィード（クリックで地図連動） |
| `plants.html` / `plants.js` | **ハイブリッド図鑑**：厳選15種 ＋ コミュニティ発見種／🔓発見済み・🔒未発見（Pokédex）／発見カウンター／フィルタ（すべて・見つかった・未発見・在来・外来） |
| `report.html` / `report.js` | 投稿フォーム：写真撮影＋圧縮／地点選択マップ／GPS／**EXIF位置サジェスト「ここですか？」**／**BioCLIP 候補から種選択**／**「じゃじゃーん」発見演出** |
| `data.js` | `PLANTS`（15種）＋ `SIGHTINGS`（サンプル10件）＋ localStorage 保存（`saveSighting` / `getAllSightings` / `getPlantById`） |
| `style.css` | ハワイ自然色テーマ（深緑・火山黒・Lehua赤・砂） |
| `../bioclip/api.py` | **BioCLIP 2.5（`imageomics/bioclip-2.5-vith14`・ViT-H/14）** の FastAPI 推論サーバー。`/classify`・CORS有効・候補は `hawaii_plants.csv`（3,191種・学名のみ）。**さくらのサーバー(さくらインターネット)で稼働**（現在 `http://163.43.183.200:8000`）。 |

### AIモデルの要点（U22説明用）
BioCLIP 2.5 は TreeOfLife-200M（約2億枚）で学習。分類階層を丸ごと学習するため**未知の種にゼロショット汎化**する（原著 BioCLIP：CVPR 2024・arXiv:2311.18803／BioCLIP 2：arXiv:2505.23883）。→ 追加学習なしでハワイ固有種を識別できる根拠。

---

## 決定済みの設計判断

- **バニラJS継続**：フェーズ1は書き直さない。フレームワーク乗換（Astro/React等）は「フェーズ1→2の境目」で検討。
- **図鑑の画像**：Wikipedia参考写真を表示 → **発見されたら投稿写真に自動差し替え**（ハイブリッド）。
- **地図**：ハワイ諸島に限定（`ISLANDS_BOUNDS`＝初期表示、`PAN_BOUNDS`＝余白込みパン範囲）。タイル外は海色。
- **懸賞/報酬**：現金は非推奨（盗掘誘導・決済リスク）。**換金不可のポイント**で。希少種は対象外＋**座標ぼかし**。
- **実績（レピュテーション）は「質」で**：投稿数の荒稼ぎ防止。確認された同定・高評価を重く。
- **文化情報は先住民データ主権に配慮**（moʻolelo 等は実践者/長老の確認経路）。
- **モバイルは別UIを作らず 1つをレスポンシブ（mobile-first）**。
- **AIは自前サーバー（さくらのGPUサーバー）で運用**＝クラウドAI APIに非依存・データを自分で管理（旧方針「ローカルGPU優先」から、常時稼働できるさくらのサーバーへ移行）。公開時は HTTPS化（mixed content対策）＋APIキー＋レート制限＋CORS限定が必須。

---

## 将来構想ロードマップ

### ★ 背骨：3つの機能は「信頼レイヤー」を共有する
「誰が信頼できる貢献者か」というレピュテーション基盤を1つ作ると、下の ①③④ が同時に解錠される。

```
                    ┌─ ③ 確認済み写真のベクトル few-shot（AI精度↑）
アカウント(Auth)     │
   + DB(Firestore) ─┼─ ① コミュニティ検証（未確認→確認）
   + 実績レイヤー    │
                    └─ ④ 実績アカウントによる Wiki 型 説明記載
```
**依存の大原則**：Firebase（Auth＋Firestore＋Storage）＝フェーズ2が、ほぼ全ての将来機能の前提。

### 実装優先度（推論結果）

**最優先（フェーズ1内・低コスト高効果）**
- ✅ **EXIF位置サジェスト**（実装済み）
- ✅ **じゃじゃーん発見演出**（実装済み）
- ⬜ **複数植物 → 手動クロップ → 個別鑑定**（現APIのまま可・デモ映え）
  - ⚠️ **クロップは「1株まるごと」単位**（葉/種のパーツ単体はNG）。BioCLIPは丸ごとの生物で学習しており、**パーツ単体のゼロショット種同定は弱い**（論文：Medicinal Leaf等は few-shot なら高精度だが zero-shot のパーツは不安定）。species-from-part はそもそも本質的に曖昧。
  - **リスクを消す設計**：①UIで「1株を囲む」誘導＋最小クロップサイズ ②クロップは任意（既定は写真全体・複数のときだけ）③**確信度ゲート**＝キツく切りすぎ→低確信→「未確認/コミュニティ検証」に回す（＝自信満々の誤答を出さない）。保険として「全体＋クロップ」両方を分類して比較も可。
  - 本命は**自動セグメンテーション（パーツでなくインスタンス単位で切る＝下記②B）**。手動は"つなぎ"。
  - **📌 実測知見（2026-07・黄ハイビスカスで検証）＝クロップ範囲で結果が激変する**：
    - 写真＝黄ハイビスカス（*Hibiscus brackenridgei*・州花・固有種）2株＋背後に *Heliotropium anomalum*。
    - **全体で判定**：`Abutilon menziesii 47% / Abutilon sandwicense 18% / Hibiscus ovalifolius 16% / 黄ハイビスカス 9%`。全候補が**アオイ科（Malvaceae）**＝**科までは正解、種で近縁固有種と混同**。正解は候補内に居る（4位9%）が1位でない。
    - **花1輪だけにクロップ**：1位が *Abutilon menziesii* になったり黄ハイビスカスになったり、**わずかな範囲変更で argmax が入れ替わる**。
    - **原因**：アオイ科の花は雄しべ柱状融合の**共通花構造**でクローズアップだと似る。花だけに寄せると**葉形・株の姿という決定的手がかりを失いパーツ化** → 学習データで優勢な種に転ぶ（＝論文の「パーツ単体ゼロショットは不安定」の実演）。葉・株を含めると *Hibiscus* 側に寄る。
    - **設計への含意（3つ）**：①クロップは「花だけ」でなく**葉＋株の姿を含める**（＝「1株まるごと」の実体はこれ）。②**クロップを動かして結果がバラつく＝低確信シグナル** → 未確認/コミュニティ検証へ。「アオイ科の可能性大だが種は未確定」と正直表示。③ロバスト化の本命＝**複数ビューの確率平均（TTA・下記）**。
    - **なぜ弱点でなく強み**：「単一クロップは不安定と実測 → 多層（1株誘導＋確信度ゲート＋複数ビュー平均＋コミュニティ検証）で守る」はモデルを理解して扱う証拠＝U22テク枠・エッセイの核。同じアオイ科の固有種2種を切り分ける、というハワイ文脈のストーリーも立つ。
  - ⬜ **複数ビュー平均（Test-Time Augmentation）＝クロップ感度への対策候補**：1枚のクロップに賭けず「全体＋クロップ（複数）」を各`/classify`して**確率ベクトルを平均** → 単一クロップのブレを打ち消す。`report.js`側で複数回叩いて平均するだけ・サーバー改修不要。
    - **⚠️ 将来的に不要になるかも**：③の**確認済み写真ベクトル few-shot（kNNブレンド）が入ると、実写真の錨で近縁種を切り分けられる**ため、TTAで無理に安定化する必要が薄れる可能性。上の実測でも正解は候補内（4位9%）に居るので、few-shotの再ランキングで1位に押し上げられる射程。→ **TTAは"つなぎ"、本命はベクトル埋め込み（③）**。優先度は低め・様子見。

**基盤（次の一手）**
- ⬜ **⑥ Webデータベース化（Firebase）＝フェーズ2**：投稿の共有・永続化。写真は Storage に置きURLをDBへ（Firestoreは1MB上限）。`saveSighting`/`getAllSightings` を差し替えるだけで画面はほぼ不変。
- ⬜ **⑥ モバイル仕上げ（レスポンシブ＋ボトムナビ）→ PWA**（ホーム追加・オフライン）。フィールドアプリなので早め推奨。

**基盤の後（データが貯まってから・高インパクト）**
- ⬜ **③ 確認済み写真のベクトル few-shot（Tip-Adapter）**：確認済み投稿の画像ベクトルを保存し kNN でゼロショットとブレンド。原著で 39%→69% の実証。前提＝DB＋検証＋データ量。規模的に専用ベクトルDBは不要（numpy総当たりで十分）。
- ⬜ **① 共起予測クエスト＋ポイント**：「この植物の近くにこの種がいるはず」を生態学的共起で提示しクエスト化。**現金でなくポイント**。外来種の早期発見に向けると保全的にも安全。
- ⬜ **④ 実績アカウントの Wiki 型説明**（下記）。
- ⬜ **② 複数植物の自動セグメンテーション（SAM/FastSAM/GroundingDINO等）**：サーバー側でクロップ。手動クロップの上位版・フェーズ3。
- ⬜ **⑦ 時系列トレンド（植物の増減を可視化）**：下記に深掘り。原案の「統計ダッシュボード＋RODヒートマップ」の具体化。
- ⬜ **⑧ 埋め込み可視化（Embedding Map）**：下記に深掘り。写真ベクトルを2D散布図に。U22テクノロジー枠の差別化。

**図鑑の拡張計画**
- ⬜ **⑤ 図鑑の全種化（3,191種へ拡張）**：下記に深掘り（具体手順・データ源・3層コンテンツモデル）。原則：*図鑑に載る種 = AIが認識できる種* で一致させる。

### ④ 実績アカウントによる Wiki 型 説明記載（段階案）
- Lv1：ログインユーザーが説明を「提案」→ 管理者が承認（レビュー行列）
- Lv2：実績スコア導入。信頼ユーザーは即反映、それ以外はレビュー
- Lv3：編集履歴・差し戻し（本格Wiki）＋**文化情報は別レビュー経路**
- 注意：出典必須（誤情報対策）／実績は質ベース／希少種の位置ぼかし継続。

**AI一次審査（AIモデレーション）＝Lvモデルに差し込む一次フィルター**
- **原則：AIは"最終承認者"でなく"一次トリアージ"**。文章のトーン・形式・明らかな矛盾・出典照合はAIが判定できるが、**事実の真偽は保証できない（ハルシネーション）** → AI単独承認は危険。
- **AI一次審査の3分類**：①自動承認候補（形式OK・出典あり・事実中立・低リスク）②人間レビュー行列（**文化情報**／出典なしの主張／AIが不確実）③却下（スパム・攻撃・プロンプトインジェクション）。
- **堅くする2工夫**：①**出典必須にしてAIに「出典と主張が一致するか」を照合させる**（記憶からの承認でなく引用元と突き合わせ＝真偽検証に近づく・審査で説明しやすい）。②**文化情報は問答無用で人間経路**（AIは「文化情報だから実践者レビューへ」と振り分けるだけ・先住民データ主権）。
- **レピュテーション層と合成**：信頼ユーザー＋AIグリーン→即公開／低実績 or リスク→人間キューへ（Lv2が自然にAIと噛み合う）。差し戻し（Lv3）で誤承認も可逆。
- **モデル選定（決定）**：
  - **本命＝Claude Haiku 4.5 でトリアージ**（安い $1/$5・速い・指示追従◎・構造化出力ネイティブ・出典照合に web fetch サーバーツールが使える・VRAM消費ゼロ）。
  - **難ケース（文化情報・出典照合が微妙）だけ Claude Sonnet 5 にエスカレーション**（$3/$15・高品質）。Opus 4.8 は過剰でトリアージには不要。
  - **データを外に出したくないなら 自前LLM（Llama 3.x / Qwen2.5 / Gemma の4bit量子化7〜9B）**：無料・データが自分の管理下・BioCLIPと同じ「自前サーバーで動くAI」で一貫。⚠️ さくらのGPUをBioCLIP（ViT-H/14）と共有するなら同時起動でVRAMを奪い合うため、**用途で切り替え**か別インスタンスが現実的。ハイブリッド（自前で一次→割れたらHaiku）でコストと自己管理の両取りも可。
  - 参考：Gemini Flash（非常に安い・多言語だがハワイ語の質は未知数）も候補だが別エコシステム。
- **依存・位置づけ**：④のWiki（＝DB＋アカウント＝フェーズ2以降）が前提。今はモデル選定を決めておけば十分、実装は先。

### ⑤ 図鑑の全種化：3,191種への拡張（深掘り・具体手順）

**ゴール**：図鑑を「AIが認識できる全種（`hawaii_plants.csv` = 3,191種）」に拡張。ただし現状CSVは**学名のみ** → **メタデータのエンリッチ（在来/外来・写真・保全 等）が作業の本体**。

**方針（決定事項）**
- **master = 全3,191種**（AIと一致）。ただし**小図鑑で構造化**：`在来スターター(現15)` / `在来 全体` / `外来ウォッチ` / `全種`。進捗と動機は在来に寄せる。
- **3層コンテンツモデル**：
  - **Tier A（厳選リッチ・手作業・検証済み・現状15＋α）**：ハワイ語名・文化・保全・写真フル。
  - **Tier B（自動エンリッチ・数百〜全種）**：学名＋在来/外来＋写真＋短い説明＋保全。**「自動・未検証」ラベル**必須。
  - **Tier C（コミュニティ拡充）**：発見・検証でハワイ語名・文化・良い写真を追加（アイデア④のWiki）。B→A方向へ育つ。

**グルーピングとフィルタの方針（決定）**
- **主軸 = 在来/外来**（使命に直結・単純・進捗の動機）。トップの大分類・小図鑑はこれで切る。
- **従 = 分類（科・目）**：ブラウズ/学習向けの**2軸目のサブフィルタ**（後付け）。3,191種を科でトップ分割すると細かすぎ・ラテン語で観光客に伝わらないため主軸にはしない。分類データは自動取得が安く（POWO/GBIF、属＝学名の先頭語）、BioCLIPの「生命の樹」構造とも一致＝テク的にも綺麗。
- **フィルタは段階実装**：
  1. **必須（図鑑と"同時"に作る）**：検索＋在来/外来＋遅延描画。無いと3,000枚が"使えない壁"になるため**後付け不可**。
  2. **高度（後から反復追加）**：科・目・島・保全ステータス別。規模が出てから効く。
  3. **前提**：どのフィルタもメタデータのエンリッチ（Tier B）が済んでいること。
- **注意**：今の15種のために凝ったフィルタを作り込まない（フィルタ投資は3,000種と足並みを揃える）。

**エンリッチのデータ源（自動化）**
- 在来/外来（endemic / indigenous / naturalized / invasive）：**POWO（Kew, Plants of the World Online）API**（ハワイでnative/introduced）＋ **USDA PLANTS**。不完全なので provenance を持たせ community 修正可。
- 保全ステータス：**IUCN Red List API** ＋ 米国ESA。
- 写真＋短い説明：**Wikipedia REST API**（実施済みの方法）。約半数は画像あり。ライセンス（CC BY-SA）帰属を明記。iNaturalist API も候補。
- ハワイ語名：自動化困難（Wagner's *Manual of the Flowering Plants of Hawaiʻi* / Hawaiian Dictionary / native-plants-hawaii.org）→ **半手動 or コミュニティ**。雑草/作物は名前なしが普通（「—」でOK）。
- 文化情報：**自動スクレイプしない**（先住民データ主権）。Tier A / コミュニティのみ。

**技術（実装の形）**
- `bioclip/` 側の **Python スクリプト**で `hawaii_plants.csv` → 各APIを叩いて **`plants_all.js`（`const PLANTS_ALL = [...]`）を生成**。※JSONを`fetch`すると `file://` で CORS に当たるので、`data.js` と同じ「グローバル代入するJSファイル」形式にする。
- 各エントリ：`{ scientificName, nativeStatus, hawaiianName?, iucnStatus?, imageUrl?, shortDesc?, source, verified:false }`。
- `data.js`：Tier A（厳選 `PLANTS`）を Tier B（`PLANTS_ALL`）に**上書きマージ**（同一学名は A 優先）。発見判定・照合は**学名で統一**。

**UI（3,000枚をどう見せるか）**
- 「**全種 All**」タブ ＋ **検索ボックス**（学名/ハワイ語名）。
- **遅延描画**（全カードを一度に描かない・スクロール追加 or ページング）。
- **小図鑑バー**：在来スターター / 在来全体 / 外来ウォッチ / 全種、それぞれ達成率。
- 発見状態は今まで通り投稿（`sightings`）から。Tier B も 🔓/🔒。

**段階（実行順）**
1. スコープ確定（全3,191・小図鑑構造）← 本メモで決定。
2. **エンリッチ・パイプライン v1**：POWO＋Wikipedia＋IUCN で `plants_all.js` 生成（在来/外来＋写真＋保全）。ハワイ語名・文化は空でOK。
3. **UI**：全種タブ＋検索＋遅延描画＋小図鑑バー。Tier A は「featured」。
4. **品質**：provenance表示・「自動/未検証」ラベル・community修正導線。
5. **Tier C**：発見/検証でハワイ語名・文化・写真を community が追加（④と接続・DB後）。

**リスク/注意**
- 自動の在来/外来は不完全 → **「自動・未検証」明示＋修正可**（断定しない）。
- 画像ライセンス（CC BY-SA）の**帰属表示**。
- パフォーマンス（**遅延描画必須**）。
- 文化情報は自動化しない（データ主権）。
- **Tier B は DB 不要**で作れる（発見状態だけ `sightings` 依存）。まずローカルで全種図鑑の骨格を作れる。

### ⑦ 時系列トレンド：植物の増減を可視化（深掘り）

**概要**：各投稿が持つ `date`（YYYY-MM-DD）を使い、種ごと・分類ごとの**目撃報告数の推移**をグラフ化。「増えている種／減っている種」「在来 vs 外来の割合の推移」「ROD報告の推移」を見せる。原案の「統計ダッシュボード（Chart.js）＋RODヒートマップ」を具体化・深掘りしたもの。ナビの「統計 Stats」枠（現在は無効）に載せる。

**なぜ価値が高いか**
- アプリの使命（外来種の侵食・RODの早期発見）は**時間による増減でこそ見える**。生の投稿データを"気づき"に変える機能。
- エッセイの核「消えていく記録 → 時系列で追える」に直結。
- U22：技術（集計・可視化）＋アイデア（保全モニタリング）の両軸に効く。

**⚠️ 最重要の注意：観測努力バイアス（ここを外すと"嘘のグラフ"になる）**
市民科学の目撃数は**真の個体数ではない**。「投稿が増えた」は「植物が増えた」ではなく、単に**観測者・投稿が増えただけ**かもしれない。これを設計で扱うと科学的に誠実になり、審査でも評価される。
- **「観測トレンド（目撃報告数）」と正直にラベル**する（"個体数"とは言わない）。
- **観測努力で正規化**：全投稿に占める種Xの割合／アクティブユーザーあたりの投稿数、など。
- **構成比の推移**（在来 vs 外来のシェア）は、生カウントより頑健。
- **occupancy（目撃された"地点数"）**を使うと、生カウントより頑健。
- **季節性**（開花・結実期で見つけやすさが変わる）を考慮 → 前年同月比や移動平均で。
- **小サンプルは断定しない**（最低件数の閾値・「データ不足」表示）。

**計算する指標**
- 種 × 期間（月/四半期）の件数
- 直近期間 vs 前期間の**変化率（％）**
- **増加ランキング／減少ランキング**
- 在来シェア vs 外来シェアの推移
- 新規地点数（occupancy 代理）
- 外来が急増 → **早期警戒アラート**（使命に直結）

**可視化（Chart.js）**
- 折れ線：月次の目撃数（種別／分類別）
- 積み上げエリア：在来 vs 外来の構成比推移
- 種カードに**トレンド矢印（↑↘）＋変化率**
- ROD：ʻŌhiʻa の ROD 疑い報告の推移・ヒートマップ（原案のRODヒートマップ）
- 時空間：外来種の新規目撃が月ごとに**どこに現れるか**を地図でアニメ表示

**依存・段階**
- 必要：投稿の `date`（✅既にある）＋データ量（→ DB/フェーズ2＋蓄積）＋Chart.js。
- MVPは今のサンプル＋localStorageでも作れる（データは薄いがデモ可）。本領はDB導入後。
- 位置づけ：フェーズ3（原案の統計ダッシュボード＝これ）。

**リスク**
- 観測努力バイアス（上記・最重要）／小サンプルのノイズ／季節性の交絡。
- 誤読（"減少"を実個体数の減少と誤解）→ ラベルで明示。
- 希少種：減少表示は価値ある一方で座標ぼかしは継続。

**将来**
- 外部データ（標本記録・GBIF）と突き合わせて真のベースライン化。
- 外来種の**拡散予測**（① 共起クエストと連携）。
- 観測努力を補正した occupancy モデル（本格生態学）。

### ⑧ 埋め込み可視化（Embedding Map）：写真ベクトルを"見える化"（深掘り）

**概要**：BioCLIP が各写真から作る高次元ベクトル（埋め込み）を **t-SNE / UMAP で2Dに次元削減**し、散布図として表示。1点＝1枚の写真、色＝種や在来/外来。似た植物ほど近くに固まる「植物の類似マップ」。BioCLIP 原著の t-SNE 図（Fig 3）を**ハワイのデータで再現**する形。

**なぜ U22 テクノロジー枠に効くか**
- **見えないAIを"見える化"**できる。ʻŌhiʻa が固まる／在来と外来が分かれる、が目で分かり記憶に残る。
- 「APIを叩いた」でなく「**埋め込み空間を理解して扱っている**」証明。テク枠が評価する核心。
- 選択した **`39%→69%`（few-shot改善）を視覚で説明**：新しい写真が確認済みクラスタの近くに落ちる＝だから当たる。
- 原著自身が t-SNE で「階層を学習した」と証明済み → 同手法の再現は**信頼度が高い**。

**⚠️ 落とし穴（外すと逆効果）**
- **中身が伴わないと"飾り"**。明確なクラスタ・在来/外来分離・外れ値など「意味」が見えて初めて加点 → **データ量が要る**（コールドスタート）。
- **実機能に紐付ける**（装飾で終わらせない）：
  - 「**類似マップ**」：似た植物が近くに並ぶ／点クリックで写真表示。
  - 「**検証補助**」：未確認写真が**どのクラスタ近くに落ちるか**で確からしさを可視化。曖昧なら「コミュニティ確認へ」。
  - 「**誤同定の検出**」：種Xとされた写真が種Yの塊に紛れる → レビュー対象に（QAとして実用）。
  - 「**在来 vs 外来の分離**」を保全ストーリーとして。
- **正直さ**：2D投影は距離を歪める → 「クラスタは定性的（正確な距離ではない）」と明記すると信頼される。

**技術的な現実味**
- サーバーは既に画像ベクトルを作れる（`create_image_features`）。
- **t-SNE/UMAP はサーバー側で事前計算** → 2D座標だけブラウザへ送る。
- 描画：点が少なければ **Chart.js の散布図**で十分。多くなれば regl-scatterplot / deck.gl。

**近道（データが少ない今でも作れるデモ用の一枚）**
厳選15種の参考画像（＋各種複数枚）を BioCLIP でベクトル化 → t-SNE で散布図にすれば、コミュニティ投稿が貯まる前でも「在来/外来が分かれるクラスタ図」という**説得力あるデモ素材**が作れる。プレゼンのスライド1枚として即戦力。

**依存・位置づけ**
- ③（確認済み写真のベクトル few-shot）と**同じ埋め込み基盤**に乗る。③の延長で実装可能。
- 審査軸：テクノロジー◎／アイデア◯／プロダクト△→◯（“意味ある機能”に紐付ければ）。

---

## サーバー公開メモ（AI API＝さくらのサーバー／ドメイン購入予定）
- **AI API（BioCLIP）＝さくらのサーバー（さくらインターネット）で稼働**。現在 `http://163.43.183.200:8000`（163.43.x.x はさくらのIP帯・固定グローバルIP）。
- ドメインは「住所」であってホスティングではない。フロント（静的）＝GitHub Pages/Netlify/Cloudflare Pages（無料・HTTPS自動）、AI API＝さくらのサーバーを外部公開。
- **HTTPS mixed content**：フロントがhttpsだと `http://163...` は呼べない → AI API も HTTPS 必須。
- **HTTPS化の推奨**：さくらは固定グローバルIPがあるのでトンネル不要。**独自ドメイン＋Let's Encrypt（Nginx等でリバースプロキシ）で正規HTTPS化**するのが素直。※自己署名証明書だとブラウザで `ERR_CERT_AUTHORITY_INVALID` になる。
- 公開前に必ず：**APIキー・レート制限・CORSを自ドメイン限定**（今は `*`）。さくらのサーバーは常時稼働が前提。
- U22デモだけなら公開不要（同一ネットワークのPC＋スマホでも成立）。
